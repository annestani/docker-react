# sudo permissions rfequired
sudo: required
services:
  - dockerfile

before_install:
  - docker build -t annestani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true annestani/docker-react npm run test -- --coverage

language: generic 



